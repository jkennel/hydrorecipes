% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_recipes.R
\name{step_transport_ogata_banks}
\alias{step_transport_ogata_banks}
\title{step_transport_ogata_banks}
\usage{
step_transport_ogata_banks(
  .rec,
  time,
  distance,
  concentration_initial = 1,
  velocity = 0.1,
  diffusion = 0.1,
  retardation = 1,
  decay = 0,
  role = "predictor",
  ...
)
}
\arguments{
\item{.rec}{the R6 recipe object.}

\item{time}{vector time}

\item{distance}{vector x position}

\item{concentration_initial}{double concentration}

\item{velocity}{double velocity}

\item{diffusion}{double diffusion coefficient}

\item{retardation}{double retardation coefficient}

\item{decay}{double decay coefficient}

\item{role}{character - the name of the role}

\item{...}{additional arguments}
}
\value{
Ogata-Banks solution for time and distance pairs
}
\description{
Ogata, A., Banks, R.B., 1961. A solution of the differential equation of
longitudinal dispersion in porous media. U. S. Geol. Surv. Prof. Pap. 411-A.
1-D, infinite source, uniform flow, constant parameters, decay, retardation

To have values match the excel sheet
https://www.civil.uwaterloo.ca/jrcraig/pdf/OgataBanks.xlsm the decay
coefficient needs to be scaled by the retardation coefficient.

Care must be taken so that input values do not lead to NaN. -Need to fix this.

1-D
infinite source
uniform flow
constant parameters
no decay
no retardation
}
\examples{

formula <- as.formula(y~x)
rows <- 100

dat <- data.frame(expand.grid(as.numeric(1:rows), as.numeric(1:10)))
names(dat) <- c('x', 'y')
frec1 = recipe(formula = formula, data = dat) |>
  step_transport_ogata_banks(time = x, distance = y) |>
  plate("dt")

}
\references{
Ogata, A., Banks, R.B., 1961. A solution of the differential equation of
  longitudinal dispersion in porous media. U. S. Geol. Surv. Prof. Pap. 411-A.
  1-D, infinite source, uniform flow, constant parameters, decay, retardation
}
\seealso{
Other transport:
\code{\link[=step_transport_fractures_heat]{step_transport_fractures_heat()}},
\code{\link[=step_transport_fractures_solute]{step_transport_fractures_solute()}}
}
\concept{transport}
