% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_recipes.R
\name{plate}
\alias{plate}
\title{plate}
\usage{
plate(.rec, type = "dt")
}
\arguments{
\item{.rec}{the R6 recipe object.}

\item{type}{the return type for the recipe (dt = `data.table`, df = `data.frame`,
tbl = `tibble`, list = `list`, m = `matrix`)}
}
\description{
Get the results from the recipe. If the recipe hasn't been prepped and
  baked, this will do those steps and return the result.
}
\examples{

dat <- data.frame(x = rnorm(10), y = rnorm(10))

rec <- recipe(y~x, data = dat) |>
       step_scale(x) |>
       prep() |>
       bake() |>
       plate()

rec <- recipe(y~x, data = dat) |>
       step_scale(x) |>
       plate()

}
