% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_recipes.R
\name{step_fft_coherence}
\alias{step_fft_coherence}
\title{step_fft_coherence}
\usage{
step_fft_coherence(.rec, terms, role = "augment", ...)
}
\arguments{
\item{.rec}{the R6 recipe object.}

\item{terms}{the unquoted names of the variables to use or a selector
function.  terms replaces the `...` of the recipes package but requires
variables to be included within `c()`.  For example to include variables x
and y you would write `c(x,y)` in the hydrorecipes package.}

\item{role}{character - the name of the role}

\item{...}{additional arguments}
}
\value{
an updated recipe
}
\description{
estimates the coherence between terms.
}
\examples{

data(kennel_2020)

form <- as.formula("wl~.")

formula <- as.formula(wl~baro + et)
frec = recipe(formula = formula, data = kennel_2020) |>
  step_fft_pgram(c(wl, baro, et), spans = 7) |>
  step_fft_coherence() |>
  prep() |>
  bake()

}
