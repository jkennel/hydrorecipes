% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_recipes.R
\name{step_fft_transfer_experimental}
\alias{step_fft_transfer_experimental}
\title{step_fft_transfer_experimental}
\usage{
step_fft_transfer_experimental(
  .rec,
  terms,
  spans = 3,
  detrend = TRUE,
  demean = TRUE,
  taper = 0.1,
  n_groups = 200,
  time_step = 1,
  formula = NULL,
  role = "augment",
  ...
)
}
\arguments{
\item{.rec}{the R6 recipe object.}

\item{terms}{the unquoted names of the variables to use or a selector
function.  terms replaces the `...` of the recipes package but requires
variables to be included within `c()`.  For example to include variables x
and y you would write `c(x,y)` in the hydrorecipes package.}

\item{spans}{vector of odd integers giving the widths of modified
    Daniell smoothers to be used to smooth the periodogram.}

\item{detrend}{logical. If \code{TRUE}, remove a linear trend from
    the series. This will also remove the mean.}

\item{demean}{logical. If \code{TRUE}, subtract the mean of the
    series.}

\item{taper}{specifies the proportion of data to taper.  A split
    cosine bell taper is applied to this proportion of the data at the
    beginning and end of the series.}

\item{n_groups}{number of results}

\item{time_step}{\code{numeric} monitoring interval size}

\item{formula}{formula notation to specify inputs and outputs}

\item{role}{character - the name of the role}

\item{...}{additional arguments}
}
\value{
an updated recipe
}
\description{
Calculates the transfer function with pgram results.
}
\examples{
data(kennel_2020)

form <- as.formula("wl~.")

rec <- recipe(form, kennel_2020) |>
       step_fft_transfer_experimental(c(wl, baro, et), spans = 3) |>
       plate()

}
